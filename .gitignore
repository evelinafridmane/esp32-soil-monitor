# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# IDE
.vscode/
.idea/

# Personal project notes (not for public repo)
CLAUDE.md

# Claude Code local state (lock files, settings)
.claude/

# MicroPython firmware binary (re-download if needed)
ESP32_GENERIC_C3-*.bin

# Secrets — never commit
secrets.py
.env
*.secret

# Per-machine firmware config (real values; commit config.example.py instead)
firmware/config.py
